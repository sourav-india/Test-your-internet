{
  "name": "pulse-speed-test",
  "version": "1.0.0",
  "private": true,
  "description": "A real browser-to-server internet speed test",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "engines": {
    "node": ">=18"
  }
}
